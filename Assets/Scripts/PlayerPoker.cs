using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class PlayerPoker : MonoBehaviour
{
    public GameObject launcher;
    string data1;
    public Text name, coins, bet;
    // Start is called before the first frame update
    void Start()
    {
        // data1 = launcher.GetComponent<"Launcher">;
        //Launcher l = launcher.GetComponent<Launcher>();
        // name.text = "fadsfds";
        //launcher.inputField1.text = "";

       // data1 = launcher.ToString();
        //Debug.Log(data1);

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
